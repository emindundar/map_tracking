name: maptracking
description: "A new Flutter project."
version: 1.0.0+1
environment:
  sdk: ^3.10.7
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # Map
  flutter_map: ^8.2.2
  # Cooridnates Calculator
  latlong2: ^0.9.1
  # URL Launcher
  url_launcher: ^6.3.2
  # Geolocator
  geolocator: ^14.0.2
  # Map Animations
  flutter_map_animations: ^0.9.0
  # HTTP
  http: ^1.6.0
  # State Management
  flutter_riverpod: ^2.6.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true